groups:
  - name: "permission.saas.tenant.group"
    side: HOST_ONLY
    def:
      - name: "permission.saas.tenant.any"
        namespace: "saas.tenant"
        action: "*"
        internal: true
        side: HOST_ONLY
      - name: "permission.saas.tenant.read"
        namespace: "saas.tenant"
        action: "read"
        side: HOST_ONLY
      - name: "permission.saas.tenant.create"
        namespace: "saas.tenant"
        action: "create"
        side: HOST_ONLY
      - name: "permission.saas.tenant.update"
        namespace: "saas.tenant"
        action: "update"
        side: HOST_ONLY
      - name: "permission.saas.tenant.delete"
        namespace: "saas.tenant"
        action: "delete"
        side: HOST_ONLY

  - name: "permission.saas.plan.group"
    side: HOST_ONLY
    def:
      - name: "permission.saas.plan.any"
        namespace: "saas.plan"
        action: "*"
        internal: true
        side: HOST_ONLY
      - name: "permission.saas.plan.read"
        namespace: "saas.plan"
        action: "read"
        side: HOST_ONLY
      - name: "permission.saas.plan.create"
        namespace: "saas.plan"
        action: "create"
        side: HOST_ONLY
      - name: "permission.saas.plan.update"
        namespace: "saas.plan"
        action: "update"
        side: HOST_ONLY
      - name: "permission.saas.plan.delete"
        namespace: "saas.plan"
        action: "delete"
        side: HOST_ONLY